# Project Progress

Last updated: 2026-08-04

This file reports delivery of the repository and website. It does not award competency. Learner evidence and levels remain in `progress/ledger.md`.

## Snapshot

| Field | Current value |
|---|---|
| Branch | `main` |
| Latest committed substantive checkpoint | `cc6dcbf` - quarantined `LES-0037` infrastructure-as-code foundations candidate, three assessments, fifteen official or authoritative references, bounded seven-case deterministic model, explicit Ubuntu/provider/infrastructure-evidence blockers, and no live-reader publication |
| Remote | `origin` is configured for this dedicated learning-path repository |
| Source checkpoint parity | Repository checkpoint `cc6dcbf6ebe42339763d11101450a3ea47ceb68a`, tree `4fd6fbee4d8ef92e875711d1102b912dffb9718e`, was pushed with local/`origin/main`/remote parity and 787 tracked files before this exact-checkpoint documentation edit. Canonical feature `f2e3e23e51a7eb433c721c29439f3c0cce5a0893` remains the latest live-reader feature |
| Active repository | `<isolated-repository>` outside OneDrive; the prior OneDrive working copy remains untouched as a rollback source |
| Worktree | The canonical feature exposes twenty-six routed identities across Volumes 00 through 04: five legacy lessons plus twenty-one schema-backed `substantive-draft` lessons, 63 assessments—forty-two complete-answer records and twenty-one answer-isolated independent transfers—and 172 references. Content/schema/reader/lint/type/build, loopback HTTP, canonical Ubuntu lab, static contrast sampling, hygiene, commit, push, and exact remote parity gates pass; real-browser QA remains unavailable. Project validation does not change learner competency |
| Official title | `Reliability Atlas`; production HTTP, title, privacy, route, and measured static night-theme contrast assertions pass, while visual, keyboard, theme-toggle, and mobile browser QA remain unavailable and unclaimed |
| Current milestone | Canonical feature `f2e3e23` publishes `LES-0026` / `V04-L01` / `OBS-001` at `/book/reliability/observability-foundations` with three assessments, nine references, and a bounded local five-signal fixture. It remains `substantive-draft`; the fixture is not an instrumented representative service, OpenTelemetry, Prometheus/Grafana, a vendor backend, production behavior, formal acceptance, learner transfer, or mastery |
| Quarantined work in progress | `LES-0027` / `OBS-002` remains blocked on immutable locks and OpenTelemetry execution. `LES-0028` through `LES-0030` provide unpublished metrics, logging and alerting candidates. `LES-0031` through `LES-0034` provide SRE operating-model, SLO, incident-command and causal-learning candidates. `LES-0035` and `LES-0036` provide capacity/performance and resilience; `LES-0037` and `LES-0038` begin IaC and Terraform/OpenTofu. Direct/static checks pass within recorded boundaries, and LES-0038 has a passing exact-version Windows dual-CLI plan path, but WSL fails before Ubuntu startup and no reviewed representative monitoring, service/team/SLO, human incident, PIR, capacity/resilience or provider-backed IaC case has run. None of the twelve drafts is in the registry or website |
| Current learner gate | `PLAN-CUR-101` / `PLAN-LAB-102` - learner-operated inode remediation evidence is pending |
| Cloud policy | Local only; no online cloud resources |

## Completed and committed foundation

| Date | Plan IDs | Result | Evidence |
|---|---|---|---|
| 2026-07-20 | `PLAN-LAB-003`, `PLAN-LAB-102` foundations | Created a bounded ENOSPC diagnostic container and initial evidence workflow | Commits `75bc494` through `b514c6d` |
| 2026-07-31 | `PLAN-LAB-102` | Hardened the fixture to run as UID/GID 65534 and documented Docker recovery plus safe inode remediation | Commits `55544da`, `571cccb`, `8af031e` |
| 2026-08-01 | `PLAN-INT-003`, `PLAN-WEB-001`, `PLAN-WEB-006` foundations | Added target-role mapping and the first local, Git-backed learning cockpit | Commit `798404e` |
| 2026-08-02 | `PLAN-CUR-101` through `PLAN-CUR-105` initial content | Added the first five Linux foundation lessons, diagrams, guided labs, optional checks, and interview prompts | Commit `d958043` |
| 2026-08-02 | `PLAN-MS-00`, governance, architecture, routed reader, Linux-depth, lab-safety, and quality checkpoint | Added durable project controls, the canonical 107-ID curriculum map, routed/deep Linux reader, detailed decoders and answer guides, hardened ENOSPC ownership boundaries, and dependency-free content validation | Commit `aa3ede8` |
| 2026-08-02 | `PLAN-ARC-008`, `PLAN-WEB-005`, `PLAN-WEB-011`, `PLAN-AUD-005` reader checkpoint | Added five-lesson local search, bookmarks, recent/resume state, private reading markers, command-copy feedback, safe storage fallback/reset, current-page navigation, and dependency-free tests without changing mastery | Commit `836c29e`; pushed to `origin/main` |
| 2026-08-02 | `PLAN-ARC-004`, `PLAN-ARC-007`, `PLAN-QUA-002` structured-content checkpoint | Added strict lesson/assessment/reference schemas, immutable legacy identity, answer-isolated independent transfer, cross-record validation, path/symlink/case hardening, and disposable adversarial tests | Commit `4c1b922`; pushed to `origin/main` |
| 2026-08-02 | `PLAN-ARC-004`, `PLAN-CUR-106`, `PLAN-WEB-005`, `PLAN-AUD-005`, `PLAN-QUA-002` first structured-lesson checkpoint | Published schema-backed `LES-0006` as a `substantive-draft`, added its renderer/catalog adapter, three assessments, eight primary references, safe Ubuntu command cards, bounded practice, answer isolation, and parity coverage without changing any learner level | Commit `24201bb`; pushed to `origin/main` |
| 2026-08-02 | `PLAN-CUR-000`, `PLAN-LAB-100`, `PLAN-ARC-008`, `PLAN-WEB-005`, `PLAN-AUD-005`, `PLAN-QUA-002` systems-thinking checkpoint | Added schema-backed `LES-0007` as a `substantive-draft`, Volume 00 routing, cross-volume search and state migration, three assessments, eight primary references, and a bounded Ubuntu queue-model lab without changing learner evidence | Commit `817bb60`; pushed to `origin/main` |
| 2026-08-02 | `DBG-001`, `PLAN-LAB-107`, `PLAN-WEB-002`, `PLAN-AUD-005`, `PLAN-QUA-002` troubleshooting and Reliability Atlas checkpoint | Added schema-backed `LES-0008`, FRAME worksheet, three assessments, eight references, bounded verified lab, advisory prerequisites, canonical eight-lesson search, strict local launcher, truthful nine-stage homepage, title migration, and privacy scrub without changing learner evidence | Commit `22d3160`; pushed to `origin/main` |
| 2026-08-02 | `PLAN-CUR-000`, `PLAN-CUR-107`, `PLAN-CUR-108`, `PLAN-CUR-200` through `PLAN-CUR-204`, `PLAN-CUR-300`, `PLAN-CUR-301`, reader and quality workstreams | Added ten exact-18-section `substantive-draft` lessons (`LES-0009` through `LES-0018`), ten guarded Ubuntu labs, thirty assessments, eighty references, Volume 02/03 routes, eighteen-identity search/state integration, and audited documentation without changing learner evidence | Commit `142fb82`; pushed to `origin/main` with exact parity |
| 2026-08-02 | `PLAN-CUR-301`, `PLAN-CUR-302`, `PLAN-CUR-303`, generated registry, reader, lab, and reproducibility workstreams | Added five exact-18-section `substantive-draft` lessons (`LES-0019` through `LES-0023`), fifteen assessments, forty references, five guarded labs, generated registration, and twenty-three-identity reader integration without changing learner evidence | Commit `f742747`; pushed to `origin/main`, then reproduced in the isolated clone with exact revision/tree parity |
| 2026-08-02 | `PLAN-CUR-304`, `CI-001`, generated registry, reader, lab, and quality workstreams | Added the exact-18-section `LES-0024` CI/CD architecture substantive draft, three assessments, eight references, a guarded local pipeline model, and twenty-four-identity reader integration without changing learner evidence | Commit `a093474`; pushed to `origin/main` with exact revision parity and clean-archive validation |
| 2026-08-02 | `CI-002`, generated registry, reader, lab, and quality workstreams | Published the exact-18-section `LES-0025` CI platform-operations substantive draft, three assessments, eleven official references, a bounded dual-engine local lab, and twenty-five-identity reader integration without claiming provider acceptance or learner mastery | Commit `a80ca72`; pushed to `origin/main` with exact revision/tree parity and a clean worktree |
| 2026-08-02 | `PLAN-CUR-400`, `OBS-002`, draft-safety and quality workstreams | Checkpointed unpublished `LES-0027`: an exact-18-section OpenTelemetry candidate, three assessments, twelve new references, two reused reference identities, and a fail-closed bounded lab design whose independent runtime-safety blockers remain explicit | Commit `ecbfbe4`; pushed to `origin/main` with exact revision/tree parity and a clean worktree; canonical registry remains 21/63/172 |
| 2026-08-04 | `PLAN-CUR-400`, `OBS-003`, metrics-semantics and quality workstreams | Checkpointed unpublished `LES-0028`: an exact-18-section Prometheus/PromQL/Grafana candidate, three assessments, fourteen new official references, one reused canonical reference, and a bounded model whose independent, Ubuntu, and real-runtime limits remain explicit | Commit `bf2203e`; pushed to `origin/main` with exact revision/tree parity and a clean worktree at verification time; canonical registry remains 21/63/172 |
| 2026-08-04 | `PLAN-CUR-400`, `OBS-004`, logging-pipeline and quality workstreams | Checkpointed unpublished `LES-0029`: an exact-18-section structured-logging candidate, three assessments, fifteen official or primary-source references, and a bounded model whose independent, Ubuntu, and real-runtime limits remain explicit | Commit `4cdc092`; pushed to `origin/main` with exact revision/tree parity and a clean worktree at verification time; canonical registry remains 21/63/172 |
| 2026-08-04 | `PLAN-CUR-400`, `PLAN-CUR-401`, `OBS-005`, alerting and quality workstreams | Checkpointed unpublished `LES-0030`: an exact-18-section alerting, dashboards, and user-journey candidate, three assessments, fifteen official or primary-source references, and a bounded model whose independent, Ubuntu, and real-runtime limits remain explicit | Commit `93380b6`; pushed to `origin/main` with exact revision/tree parity and a clean worktree at verification time; canonical registry remains 21/63/172 |
| 2026-08-04 | `PLAN-CUR-400`, `PLAN-CUR-403`, `SRE-001`, reliability operating-model and quality workstreams | Checkpointed unpublished `LES-0031`: an exact-18-section SRE-principles, risk, toil, ownership, readiness, on-call, and learning candidate, three assessments, fifteen official references, and a bounded model whose independent, Ubuntu, organizational, and production limits remain explicit | Commit `9ebad4f`; pushed to `origin/main` with exact revision/tree parity and a clean worktree at verification time; canonical registry remains 21/63/172 |
| 2026-08-04 | `PLAN-CUR-401`, `SRE-002`, SLO decision-system and quality workstreams | Checkpointed unpublished `LES-0032`: an exact-18-section SLI/SLO/SLA, error-budget, burn-rate, low-traffic, alert, and policy candidate, three assessments, fifteen primary or official references, and a bounded nine-case model whose independent, Ubuntu, provider, stakeholder, contractual, and production limits remain explicit | Commit `e32f068`; pushed to `origin/main` with exact revision/tree parity and a clean worktree at verification time; canonical registry remains 21/63/172 |
| 2026-08-04 | `PLAN-CUR-403`, `SRE-003`, incident-command and quality workstreams | Checkpointed unpublished `LES-0033`: an exact-18-section on-call, severity, command, controlled-change, communication, handoff, recovery, causal-review, and action-quality candidate, three assessments, fifteen primary or official references, and a bounded seven-case model whose independent, Ubuntu, organizational, human, and production limits remain explicit | Commit `f2165a6`; pushed to `origin/main`; canonical registry remains 21/63/172 and no learner evidence changed |
| 2026-08-04 | `PLAN-CUR-403`, `SRE-004`, causal-analysis and post-incident-learning workstreams | Checkpointed unpublished `LES-0034`: an exact-18-section evidence, timeline, causal-graph, counterfactual, blameless-PIR, action-verification, and recurrence-learning candidate, three assessments, fifteen primary or official references, and a bounded seven-case model whose Ubuntu, evidence-governance, human-review, production, and effectiveness limits remain explicit | Commit `4795f01`; pushed to `origin/main`; canonical registry remains 21/63/172 and no learner evidence changed |
| 2026-08-04 | `PLAN-CUR-402`, `PERF-001`, capacity/performance and quality workstreams | Checkpointed unpublished `LES-0035`: an exact-18-section demand, goodput, tail-latency, queueing, knee, bottleneck, test-design, forecast, scaling, autoscaling, overload, fairness and cost candidate, three assessments, fifteen primary or official references, and a bounded seven-case model whose Ubuntu, representative-load, production, finance and learner limits remain explicit | Commit `2368260`; pushed to `origin/main`; canonical registry remains 21/63/172 and no learner evidence changed |
| 2026-08-04 | `PLAN-CUR-402`, `RES-001`, resilience and quality workstreams | Checkpointed unpublished `LES-0036`: an exact-18-section deadlines, retries, idempotency, circuits, bulkheads, overload, degradation and recovery candidate, three assessments, fifteen primary or official references, and a bounded six-case model whose Ubuntu, representative-service, datastore, correctness and learner limits remain explicit | Commit `dec8b9a`; pushed to `origin/main`; tracker checkpoint `94ef7d3`; canonical registry remains 21/63/172 and no learner evidence changed |
| 2026-08-04 | `PLAN-CUR-500`, `IAC-001`, infrastructure-as-code and quality workstreams | Checkpointed unpublished `LES-0037`: an exact-18-section desired/state/remote, graph, plan, drift, policy, approval, sensitive-artifact, partial-execution, convergence and recovery candidate, three assessments, fifteen official or authoritative references, and a bounded seven-case model whose Ubuntu, provider, backend, infrastructure and learner limits remain explicit | Commit `cc6dcbf`; pushed to `origin/main` with exact revision/tree parity; canonical registry remains 21/63/172 and no learner evidence changed |
| 2026-08-04 | `PLAN-CUR-500`, `TFM-001`, Terraform/OpenTofu language, planning and quality workstreams | Prepared unpublished `LES-0038`: an exact-18-section types, expressions, identity, graph, known/unknown values, validation, tests, saved-plan and JSON-inspection candidate, three assessments, fifteen official references and a guarded provider-free fixture with checksum-bound Terraform 1.15.8/OpenTofu 1.12.1 Windows plan evidence | Commit and remote-parity evidence follow the focused content checkpoint; Ubuntu lifecycle remains blocked by WSL `0x80070569`; canonical registry remains 21/63/172 and no learner evidence changed |
The committed baseline is useful and runnable, but it does not satisfy the complete curriculum or prove any skill beyond the evidence recorded in the learner ledger.

Commit `817bb60` adds `LES-0007` / `V00-L01` / `FND-001` as a second `substantive-draft` structured lesson, three assessments, eight references, a bounded Ubuntu 24.04 systems-thinking lab, Volume 00 routes, and cross-volume reader support. This is project-delivery evidence only.

Commit `22d3160` adds `LES-0008` / `V00-L02` / `DBG-001` as a third `substantive-draft` structured lesson. It adds the reusable `book/frameworks/FRAME.md` worksheet, `ASM-0007` through `ASM-0009` with `ASM-0009` answer-isolated, `REF-0017` through `REF-0024`, a bounded normal-user virtual incident lab, advisory prerequisite navigation, and eight-lesson catalog/search/state integration. Content/schema/reader/lint/type/build/route/link gates and mentor-operated `REL-LAB-014` pass. ShellCheck, concurrency, browser QA, formal acceptance, learner transfer, and mastery remain pending.
Commit `142fb82` adds `LES-0009` through `LES-0018`: a safe local workbench, block-I/O and isolation foundations, packet and transport paths, DNS, HTTP, TLS, Bash, and Python operational automation. The ten lessons contain approximately 148,487 whitespace-delimited words, exactly 18 required sections each, thirty assessments, eighty references, and guarded labs. Automated project gates and the recorded Ubuntu verification pass, but formal review, learner execution, real-environment transfers, delayed recall, and mastery remain open.

Commit `f742747` adds `LES-0019` through `LES-0023`: safe PowerShell automation, Go infrastructure tooling, API contracts and serialization, reproducible builds and dependencies, and OCI containers with Docker. Their bodies contain exactly 86,250 whitespace-delimited words in total and exactly 18 required H2 sections each. They add fifteen assessments, forty references, five guarded labs, and a deterministic generated registry. Automated project and mentor-operated lab checks pass, but the lessons remain substantive drafts: no formal human review, learner execution, independent retained transfer, or mastery is claimed.

Commit `a093474` adds `LES-0024` / `V03-L09` / `CI-001`: a 24,376-word CI/CD architecture chapter with exactly 18 required H2 sections, three assessments, eight primary references, and a bounded local pipeline-model lab. It traces event, definition, graph, runner, cache, artifact, identity, approval, deployment, telemetry, and user-verification boundaries. Static gates, adversarial lifecycle checks, a full Ubuntu 24.04 verifier, root refusal, exact cleanup, project gates, and the production HTTP crawl pass. The lesson remains a substantive draft; these checks do not establish formal chapter acceptance or learner mastery.

Commit `a80ca72` publishes `LES-0025` / `V03-L10` / `CI-002` at `/book/engineering/ci-platform-operations`. The chapter compares GitHub Actions, GitLab CI/CD, Jenkins, and Azure Pipelines through one mechanism-first operations model and adds three assessments, eleven official references, and a bounded dual-engine local lab. The two engines execute only purpose-built teaching behavior; they do not execute a vendor platform, validate provider authorization, or establish provider acceptance. The chapter remains a `substantive-draft`, and publication does not change learner evidence or mastery.

Feature commit `f2e3e23` promotes `LES-0026` / `V04-L01` / `OBS-001` to the canonical Volume 04 path. Its 21,486-word body has exactly 18 required H2 sections, six diagrams, twelve command cards, two labs, four incidents, three assessments, and nine references. Content and deterministic registry validation pass at 21/63/172; schema passes 38 cases with zero failures and one Windows `EPERM` capability skip; all 21 reader tests, lint, typecheck, build, the canonical Ubuntu verifier, source hygiene, and the loopback production audit pass. The HTTP audit covers 36 routes, all 26 lesson routes, two invalid-route 404s, 37 internal link targets, and 17 assets. Commit, push, and exact parity are recorded in `VER-147`; real-browser interaction remains unavailable. The deterministic fixture does not establish production or vendor behavior, causality, formal acceptance, learner execution, delayed recall, or mastery.


## Current committed checkpoint scope

| Plan IDs | Capability present on `main` | Delivery state |
|---|---|---|
| `PLAN-GOV-001`, `PLAN-GOV-003`, `PLAN-GOV-004` | Persistent control documents, book architecture, lesson/lab standard, contributor workflow, and canonical ownership rules | Committed through `a80ca72`; controls describe the twenty-lesson structured corpus and generated registration, while a fresh-contributor review and full public-release review remain |
| `PLAN-ARC-001`, `PLAN-ARC-003`, `PLAN-ARC-004`, `PLAN-ARC-006` | Volume map, typed glossary data, generated content registry, command decoders, complete teaching answers, and deeper lesson rendering | Committed through `a80ca72`; `LES-0006` through `LES-0025` are twenty schema-backed lessons, while the five legacy bodies and full curriculum acceptance remain |
| `PLAN-WEB-002`, `PLAN-WEB-003` | Routed library, dynamic lesson routes, breadcrumbs, desktop/mobile contents, separate storage practice, and the structured lesson renderer | Committed through `a80ca72`; the production HTTP audit passes 34 internal HTML routes with exactly one H1 each, 19 local assets, four invalid-route 404s, and six `LES-0025`/assessment/reference assertions; full manual navigation review remains unavailable |
| `PLAN-WEB-004`, `PLAN-WEB-008`, `PLAN-WEB-009` | Paper/night modes, text sizes, reading progress, print behavior, responsive layout, and lightweight local design | Committed through `a80ca72`; privacy and source scans pass, while formal accessibility, visual, keyboard, night-mode, mobile, print, and performance audits remain incomplete |
| `PLAN-WEB-006`, `PLAN-INT-001`, `PLAN-INT-002` | Incident, recall, teach-back, interview modes plus detailed answer guides and schema-backed assessment rendering | Committed through `a80ca72`; twenty structured lessons provide 60 validated assessments, including twenty answer-isolated independent transfers; complete banks, learner submissions, and calibrated rubrics remain |
| `PLAN-LAB-001` and `LES-0009` through `LES-0018` labs | Ubuntu-first environment cards, guarded lifecycle scripts, deterministic guided and changed-constraint cases, explicit root refusal, and exact cleanup | All twenty `lab.sh`/`verify.sh` scripts pass ShellCheck; Ubuntu syntax and all ten verifiers pass, every root probe refuses, and final `/tmp` state/residue checks are absent. Learner execution and representative real-system transfers remain |
| `LES-0019` through `LES-0023` labs | Windows PowerShell and Go, Ubuntu API/build, and Docker container-runtime fixtures with deterministic guided/independent cases, refusal boundaries, and cleanup | PowerShell/Go/Bash static gates and all five verifiers pass; the actual elevated-token PowerShell guard remains reviewer-only, and learner execution plus representative real-system transfer remain |
| `LES-0024` CI/CD architecture lab | Normal-user, local-only pipeline graph, cache/artifact identity, concurrency, failure, rollback, and cleanup model with deterministic verifier-controlled races | ShellCheck, Bash syntax, Python syntax, guided and independent cases, root refusal, stale-cache/concurrency/identity checks, replacement-race preservation, and final absence pass on Ubuntu 24.04; hosted-platform and learner transfer remain |
| `LES-0025` CI platform operations lab | Normal-user, local-only graph and stage teaching engines, artifact contracts, minimal child environment, porting mismatch, deterministic replacement races, and exact cleanup | Twelve command cards, ShellCheck, Bash/Python/JSON checks, both teaching cases, root refusal, replacement preservation, and final absence pass on Ubuntu 24.04; provider execution, production transfer, and learner mastery remain unclaimed |
| `PLAN-LAB-003`, `PLAN-LAB-102` | Pinned digest bootstrap, full v2 shell/status security envelope, removal-only reviewed-v1 envelope, `check` verifier, and descriptor-gated `reset` | Committed in `aa3ede8` and statically re-audited; Docker/WSL integration is now available, but the exact-v2, legacy-v1, and one-field full-envelope matrix still requires a fresh recorded run |
| `PLAN-LAB-106` | Non-root permissions lab and guarded cleanup | Committed in `aa3ede8`; Ubuntu normal cleanup and child-symlink refusal passed, the external target survived, and bounded cleanup succeeded. Other misuse-matrix cases remain pending |
| `PLAN-GOV-005` | Ledger records the routed reader as a project artifact without awarding learner mastery | Committed in `aa3ede8` and remains evidence-neutral |
| `PLAN-QUA-001`, `PLAN-QUA-002`, `PLAN-QUA-004`, `PLAN-QUA-006` | Lint, typecheck, production build, content/link/anchor/ID/requirement validation, registry/schema/reader tests, route/404/asset checks, privacy and source hygiene | Passed and recorded through `a80ca72`; schema reports 38 passes, zero failures, and one Windows `EPERM` capability skip, all 21 reader tests pass, and build retains only known vinext classification, Node `module.register`, and plugin-timing warnings. Offline audit reports zero vulnerabilities; browser, dependency-license, formal-review, and public-release gates remain |

## Current reader worktree scope

The committed reader checkpoint `f2e3e23` includes canonical `LES-0026`. `PARTIAL` means the complete acceptance criteria still require the named evidence; publication and project validation do not award mastery.

The target catalog exposes twenty-six routed identities across five populated volumes: five legacy lessons and twenty-one structured lessons. Generated records register lesson, assessment, reference, route, search, and learning-state identities deterministically; registry validation passes at 21/63/172 and all 21 reader tests pass. Prerequisites remain advisory; the reader does not lock access, mark prerequisites complete, or infer mastery.

| Plan IDs | Capability implemented | Remaining acceptance work |
|---|---|---|
| `PLAN-ARC-008` | Server-built cross-volume search for twenty-six lessons; canonical-worktree `LES-0001` through `LES-0026` identities, validated generated registration, passing reader/search/adjacency automation, and five-volume adjacency | Topic/role/difficulty filters, larger-corpus/fresh-clone proof, and disconnected-browser interaction proof |
| `PLAN-WEB-005` | Stable lesson links, additive device-state migration, device-local bookmarks, recent history, resume, reading markers, clear/reset flow, and origin/privacy explanation | Browser restart, real cross-tab, storage-disabled, and offline interaction tests |
| `PLAN-WEB-011` | Safe malformed/unsupported-state recovery, visible storage fallback, empty/no-result search states, four invalid-route 404s, and strict port-3000 occupied-port refusal | Dependency-install, remaining start-failure, browser-injected, and runtime error UX |
| `PLAN-AUD-005` | Fixed allowlisted state schema and repeated UI boundaries keep reading actions separate from competency across twenty-six target identities, an additive eighteen-lesson migration, and twenty-one answer-isolated transfers; all 21 reader/isolation tests pass | Full evidence-lineage, mentor-output, browser-interaction, and assessment-state audit |

## Current structured-content checkpoint

Schema v1 has strict lesson, assessment, and reference records; opaque IDs separate from aliases, routes, slugs, and curriculum IDs; permanent reservations for all five legacy lessons; deterministic generated registration; and dependency-free repository validation. Canonical feature `f2e3e23` contains twenty-one lessons (`LES-0006` through `LES-0026`), 63 assessments—forty-two complete-answer records and twenty-one answer-isolated independent transfers—and 172 primary references. The schema suite covers malformed and duplicate JSON, pinned schema policy, required/non-empty body sections, H1/title parity, CommonMark fence and raw-HTML ambiguity, URL normalization, independent answer isolation, safe lab realpaths and command policy, canonical-content and policy-file symlinks, dangling links and exact path/file case, legacy migration and collisions, canonical ownership, backlinks, and prerequisite cycles. The current suite reports 38 passes, zero failures, and one Windows `EPERM` symlink-capability skip; that skipped case must run in a capable environment before public release.

The current canonical target and generated registries contain 21 lessons, 63 assessments, 172 references, and five legacy reservations. `VER-141` records root-memory 6/6, 86 Markdown files, 51 local links, zero explicit-anchor errors, 3,132 heading anchors, 107 curriculum IDs, and requirements 46/46; `VER-142` records schema and reader results. Project validation never becomes formal chapter acceptance or learner competency evidence.

`LES-0006` / `V01-L06` / `LNX-005` is the first production lesson published through the schema-backed renderer and catalog adapter at `/book/linux/boot-kernel-systemd-journal`. Its 18 required sections, three diagrams, 12 read-only command cards, bounded guided lab, two incidents, two complete answer guides, independent transfer prompt, and reference set are marked `substantive-draft`. The five existing typed lessons remain authoritative for their routes, and their URLs and device-local IDs are unchanged.

`LES-0007` / `V00-L01` / `FND-001` is the second structured lesson at `/book/start/systems-thinking`. It adds the Volume 00 entry point, state/queue/dependency/failure-domain reasoning, three assessments, eight references, and a bounded local Ubuntu model. Commit `4c1b922` established the publishing contract, commit `24201bb` proved its first additive production path, and commit `817bb60` adds the second path. This is artifact-delivery evidence only: `substantive-draft`, publication, or a mentor-run verifier is not chapter acceptance, completed learner practice, retained knowledge, independent transfer, or learner mastery.

`LES-0008` / `V00-L02` / `DBG-001` is the third structured lesson at `/book/start/evidence-driven-troubleshooting`. It teaches FRAME from foundation through expert transfer, preserves proof boundaries, uses competing hypotheses and safe experiments, and separates mitigation, restoration, verification, causal analysis, and prevention. Its project checks and mentor-operated Ubuntu verifier pass, while formal acceptance and learner-operated evidence remain absent.

`LES-0009` through `LES-0018` add the safe workbench, Linux I/O and isolation, connectivity stack, and Bash/Python automation paths. Each has exactly 18 required sections, three assessments, eight references, and a guarded lab. Together the ten lesson bodies contain approximately 148,487 whitespace-delimited words. Passing project checks and mentor-operated verifiers do not constitute technical acceptance, learner practice, independent transfer, retained knowledge, or mastery.

`LES-0019` through `LES-0023` add PowerShell, Go, APIs and serialization, reproducible builds, and OCI/Docker foundations. Each has exactly 18 required H2 sections, three assessments, eight references, and a guarded lab. Together their lesson bodies contain exactly 86,250 whitespace-delimited words. Passing project checks and mentor-operated verifiers does not constitute human technical/instructional acceptance, learner practice, independent retained transfer, or mastery.

`LES-0024` adds CI/CD architecture: pipeline graph semantics, runner trust, cache and artifact identity, promotion, deployment control, observability, and user verification. It has exactly 18 required H2 sections, 24,376 whitespace-delimited words, three assessments, eight references, and a guarded local model. Passing the project gates and mentor-operated verifier does not constitute human technical/instructional acceptance, learner practice, independent retained transfer, or mastery.

`LES-0025` / `V03-L10` / `CI-002` adds CI platform operations at `/book/engineering/ci-platform-operations`: provider-neutral control, execution, and evidence planes; GitHub Actions, GitLab CI/CD, Jenkins, and Azure Pipelines mappings; runner trust, queues, reusable configuration, permissions, artifacts, caches, upgrades, incidents, migrations, and proof limits. It has three assessments, eleven official references, and two bounded local teaching engines. Those engines prove only their encoded local behavior, not provider behavior, authorization, security posture, acceptance, learner transfer, or mastery.

`LES-0026` / `V04-L01` / `OBS-001` adds observability foundations at `/book/reliability/observability-foundations`: five signal families, instrumentation, clocks, context, pipeline loss and reordering, backpressure, cardinality, sampling, retention, privacy, cost, RED, USE, and golden signals. It has three assessments, nine references, and a bounded local fixture. Automated project gates, the canonical Ubuntu verifier, loopback HTTP audit, source-hygiene review, feature commit, push, and exact remote parity pass, while real-browser QA remains unavailable. The fixture proves only its encoded deterministic behavior; it is not a representative service, OpenTelemetry SDK or Collector, Prometheus, Grafana, continuous profiler, vendor backend, production behavior, causality proof, learner transfer, formal acceptance, delayed recall, or mastery.

## Recorded results versus current acceptance

The repository ledger records that, earlier in this 2026-08-02 work session, six local routes returned HTTP 200, an invalid lesson returned 404, selected night-mode color pairs measured at least 5.77:1, and bounded Ubuntu labs produced cleanup proof. Those remain historical results for the worktree state at the time they ran.

For the historical `836c29e` reader checkpoint, full lint, explicit typecheck, content validation, the escalated production build, eight reader/search tests, and `git diff --check` passed. Content validation reported all six memory files, 28 Markdown files, 38 valid local links, 307 heading anchors, 107 unique curriculum IDs, and all 46 requirements. The build retained only vinext route-classification and Node `module.register` deprecation warnings. Eleven declared routes returned 200 with exactly one `h1` and no external script/link/image assets; the search payload contained all five trusted lesson IDs; the reading desk and non-mastery boundary rendered; the invalid lesson returned 404; the temporary production server listened only on `127.0.0.1:4179` and was stopped cleanly. An attempted fresh registry audit was rejected before transmission because external manifest disclosure was not authorized; `package-lock.json` is unchanged from the prior zero-advisory audit, but no new registry result is claimed.

For `24201bb`, content validation reported `root-memory=6/6`, 30 Markdown files, 42 valid local links, zero explicit-anchor errors, 383 heading anchors, 107 unique curriculum IDs, all 46 requirements, three valid schemas, one structured lesson, three assessments, eight references, and five legacy reservations. The 36-case schema suite completed with 35 passes and the documented Windows `EPERM` symlink skip; all 17 reader/search/adapter tests, lint, explicit typecheck, production build, and patch checks passed. Twelve declared routes returned 200 with exactly one `h1` and no external executable assets; the structured route exposed the metadata-derived `substantive-draft` status and `REF-0008`; search returned all six lesson IDs; the invalid lesson returned 404; and the loopback-only server stopped cleanly. An escalated Ubuntu 24.04 matrix executed the lesson's 13 exact read-only runtime checks successfully with command output suppressed. Independent content and adapter re-audits passed after parser, safety, accuracy, and reference fixes.

For `817bb60`, content validation reported `root-memory=6/6`, 32 Markdown files, 44 valid local links, zero explicit-anchor errors, 485 heading anchors, 107 unique curriculum IDs, all 46 requirements, three valid schemas, two lessons, six assessments, 16 references, and five legacy reservations. Lint, explicit typecheck, production build, all 20 reader tests, and 38 of 39 schema cases passed; the remaining case was the documented Windows `EPERM` policy-symlink skip. Fourteen declared routes returned HTTP 200 with exactly one `h1` and no external executable assets, and both invalid lesson routes returned 404. The bounded `LES-0007` lifecycle verifier passed in Ubuntu 24.04 and confirmed its state was absent after cleanup. That verifier run was mentor-operated project evidence, not learner lab completion or competency evidence.

For checkpoint `22d3160`, content validation reports `root-memory=6/6 markdown=35 local-links=47 explicit-anchors=0 heading-anchors=640 curriculum-ids=107 requirements=46/46 structured={schemas=3/3 lessons=3 assessments=9 references=24 legacy-reservations=5}`. Schema reports 38 passes plus one Windows `EPERM` skip; all 21 reader tests, lint, typecheck, and build pass with only known build warning classes. Fifteen declared routes return 200 with one `h1`, two invalid routes return 404, 15 discovered internal links are non-error, LES-0008 payload/prerequisite/assessment/reference/non-mastery checks pass, and search resolves `LES-0001` through `LES-0008`. The production listener was exactly `127.0.0.1:4186`, then stopped and the port was clear. The Ubuntu 24.04 LES-0008 verifier passes all three cases, its recorded refusal matrix, scoped answer isolation, cleanup, and absent final state. This is mentor project evidence, not formal acceptance, learner completion, or mastery.

For feature checkpoint `142fb826065b145333e5cae8a32d352468a11b34`, `HEAD` and `origin/main` matched after the non-force push. Content, schema, reader, lint, type, and build gates pass with the exact current counts above and only the known vinext classification plus Node `module.register` warning classes. ShellCheck passes all twenty new lab scripts. In Ubuntu 24.04, syntax and all ten `LES-0009` through `LES-0018` verifiers pass, all root probes refuse, final state and residue beneath `/tmp` are absent, and the three explicit `LES-0018` resumable-cleanup recovery states pass. The production HTTP audit passes 27 routes, 27 internal links, four invalid-route 404s, and five LES-0018 payload markers; exact PID 53188 was stopped and its loopback port was clear. Privacy, mojibake, conflict-marker, secret-shape, generated-residue, and raw-command-placeholder scans are clean.

For feature checkpoint `f7427478927fedcf37c7a18236b6dbce153eb9b8`, content, generated-registry, reader, schema, lint, explicit typecheck, and production-build gates pass. The schema suite reports 38 passes, zero failures, and one Windows `EPERM` file-symlink-capability skip. PowerShell parsing passes for four scripts; the `LES-0019` verifier passes Windows PowerShell 5.1 guided/independent cases, single-owner/single-contender behavior, stale-lock refusal, interruption recovery, answer isolation, and cleanup in 139.9 seconds. The elevation guard was simulated and still requires reviewer execution under an actual elevated token. Go formatting is clean and `go test ./...`, `go vet ./...`, build/profile, guided/independent, refusal, and cleanup checks pass for `LES-0020` in 102.5 seconds. Six new Bash scripts pass ShellCheck; `LES-0021` and `LES-0022` pass under Ubuntu 24.04 with root refusal, cleanup, and no residue. The `LES-0023` Docker verifier passes in 68.3 seconds using only the cached exact BusyBox digest with no pull; guided and independent PID 1 cases, eight views, five refusal classes, and cleanup pass, leaving zero lesson containers, volumes, or networks.

The production HTTP audit for `f742747` passes 32 internal HTML routes with exactly one H1 each, 19 local assets with correct CSS/JavaScript/image MIME types, four invalid-route 404s, and fifteen LES/ASM/REF marker assertions. A stale running process exposed nested hashed-asset 404s despite a correct build; the final fix normalizes Windows cache paths and supports only the legacy in-memory cache key, then requires rebuild and process restart. A fresh process passed the full audit. Privacy, mojibake, conflict-marker, secret-shape, generated-residue, and temporary-residue scans are clean.

The repository was cloned away from OneDrive to `<isolated-repository>`. The isolated clone, old OneDrive copy, and `origin/main` matched commit `f7427478927fedcf37c7a18236b6dbce153eb9b8`, tree `8b7e46c0923184ef38ad47f84628a14a9b3001c2`, and 419 tracked files; both repositories passed `git fsck --full --no-dangling`, and the isolated worktree was clean. `npm ci --offline` installed 490 packages, audited 491, and reported zero vulnerabilities. The old OneDrive copy remains untouched as a rollback source.

Post-migration listener cutover preserved both the failure and the final proof. The old OneDrive server PID 59728 was stopped. A first isolated-repository launch that used `--host` was detected listening on `0.0.0.0` and was stopped rather than accepted. The corrected process PID 22260 was launched with `--hostname 127.0.0.1 --port 3000`; listener and command-line inspection proved only `127.0.0.1:3000` and a command path beneath `<isolated-repository>`. The full 32-HTML-route/one-H1/19-asset/four-invalid-404/fifteen-marker audit then passed.

For feature checkpoint `a093474aa1907a122016f8c11b4ae0f24706a858`, a clean archive of the exact committed tree passed content validation at 79 Markdown files, 48 local links, 2,731 heading anchors, 19 lessons, 57 assessments, and 152 references; registry, all 21 reader tests, and 38 runnable schema tests passed with the documented Windows symlink skip. Lint, explicit typecheck, production build, LES-0024 static checks, the full Ubuntu 24.04 verifier, root refusal, and final absence passed. A fresh production process from that build listens only on `127.0.0.1:3000`; all 33 discovered HTML routes return success with exactly one H1, all 19 local assets have correct MIME types, four invalid routes return 404, and six LES-0024 markers render. No browser surface was available, so visual and interaction claims remain unmade.

UI feature checkpoint `e746eb8` redesigns the local reader as a restrained technical field manual: persistent book shell, section-local navigation, right-side context rail, evidence-aware reading desk, editorial code blocks, clearer reading controls, print/night/responsive states and a more navigable library. Documentation checkpoint `481a6cb` records the design system, audit, redesign plan/progress, accessibility checklist and visual-QA matrix. Content, schema, reader, lint, typecheck, production build and representative route probes passed at that checkpoint. A real browser backend was unavailable, so visual, keyboard, screen-reader, responsive and print interaction remain explicitly blocked rather than inferred from static evidence.

Quarantined `LES-0036` / `V04-L11` / `RES-001` now supplies the dependency-ordered resilience candidate after capacity. Its 18-section lesson, three assessments, fifteen primary or official references and six-case offline model cover one end-to-end deadline, cancellation, retry amplification and budgets, jitter, durable idempotency, circuit scope and probes, bulkheads, admission, rate/concurrency limits, bounded queues, shedding, degradation and recovery. Direct schemas, relationships, 50/100/100 rubric parity, model outputs, source review, ShellCheck, Git Bash syntax and scoped hygiene pass. Git Bash cannot run the lifecycle because its Python resolves to the disabled Store alias, and WSL Ubuntu fails before VM startup with `0x80070569`; no normal-user lifecycle, representative service/datastore, formal review, learner transfer, route or mastery evidence is claimed.

Quarantined `LES-0037` / `V05-L01` / `IAC-001` begins the dependency-ready infrastructure volume with a tool-neutral IaC change-system candidate. Its 8,329-word, exact-18-section lesson, three assessments, fifteen official or authoritative references and seven-case offline model distinguish desired configuration, state bindings and remote observations; explain graphs, plan consequences, drift ownership, sensitive artifacts, policy, approval, partial execution, convergence and recovery; and preserve Terraform/OpenTofu product boundaries. Direct schemas, exact 3/15 relationships, 50/100/100 rubric parity, all model cases, source resolution, ShellCheck, Git Bash syntax, canonical regressions, build and scoped hygiene pass. Git Bash lacks usable `python3`, and WSL Ubuntu fails before VM startup with `0x80070569`; no wrapper lifecycle, provider/backend/infrastructure runtime, formal review, learner transfer, route or mastery evidence is claimed.

Quarantined `LES-0038` / `V05-L02` / `TFM-001` adds a 7,240-word exact-18-section Terraform/OpenTofu language and plan candidate, three assessments, nine new and six reused official references, and a guarded provider-free fixture. It teaches HCL types and expressions, stable instance identity, graph edges, known/sensitive/unknown values, layered validation, native tests, saved plans, JSON and exact-product comparison. Checksum-matched Terraform 1.15.8 and OpenTofu 1.12.1 Windows runs both pass format, backend-disabled initialization, validation, two plan-only tests, three-create plan, JSON/graph inspection, negative variables and the new exact-address guard; no apply, state, credentials, external provider or remote resource exists, and disposable state was removed. Direct schemas, cross-draft links, ShellCheck and Git Bash syntax pass. WSL remains blocked by `0x80070569`, so no Ubuntu wrapper/refusal/cleanup runtime claim exists.

These are repository and artifact checks, not learner evidence. No in-app browser surface was available, so manual keyboard, persistence, cross-tab, clipboard, night-mode, mobile, print, visual, and disconnected interaction claims remain incomplete. Formal human review, learner evidence, delayed recall, representative real-environment exercises, dependency-license review, and public-release acceptance also remain open.

## Open findings and blockers

| ID | Severity | Affects | Finding | Required closure |
|---|---|---|---|---|
| `FIND-006` | Program gate | `PLAN-GOV-005`, `PLAN-CUR-101`, `PLAN-LAB-102` | Learner understands blocks versus inodes but has not supplied the required remediation and transfer evidence. | Learner runs the bounded remediation, preserves retained data, proves write recovery and cleanup, then completes unfamiliar transfer and delayed recall |

## Resolved findings awaiting final release review

| ID | Original severity | Resolution in the current worktree | Remaining evidence |
|---|---|---|---|
| `FIND-001` | High | Permissions cleanup requires exact non-symlink/type/UID/path checks. Ubuntu normal cleanup and a replaced-child-symlink refusal both passed; the external target survived and bounded lab cleanup succeeded. | Original finding is operationally closed; retain the transcript and complete the broader root/wrong-owner/sentinel/unexpected-entry/retry matrix as separate release coverage |
| `FIND-002` | High | A current registry-backed `npm audit --audit-level=high` reported `found 0 vulnerabilities`. The earlier install warning remains historical evidence rather than a current finding. | Retain the exact current audit result and perform the planned manual dependency-tree/license review before public release |
| `FIND-003` | Medium | Root `.gitignore` now excludes `*.tsbuildinfo`, so TypeScript incremental-build state is not treated as source. | Confirm the final build leaves no other unexplained generated files |
| `FIND-004` | Medium | All twenty `lab.sh`/`verify.sh` scripts for `LES-0009` through `LES-0018` pass ShellCheck and Ubuntu `bash -n`; all ten verifiers and root refusals pass with final state absent. | Retain the exact transcripts and complete any broader legacy-script, concurrency, hostile-state, and representative real-environment matrices required by formal review |
| `FIND-007` | Environment blocker | Docker Desktop WSL integration is available; Docker-backed `LES-0023` and local `LES-0024` verification now pass from Ubuntu 24.04. | Run and record the separate ENOSPC exact-v2, exact-legacy-v1, full-envelope counterfeit, `check`, and `reset` matrix; availability no longer blocks it |
| `FIND-008` | High | The current gate now compares current image ID, entrypoint, mounts/devices/ports, namespace modes, privilege and capabilities, security options, resource ceilings, restart/auto-remove, and exact generation-specific tmpfs options. The full legacy envelope is cleanup-only; status/shell require v2. Expanded static re-review found no remaining counterfeit path within the documented accidental-misuse threat model. | With Docker integration available, run and record one-field-at-a-time counterfeit refusal, v1 migration, v2 `check`/`reset`, and normal cleanup tests |
| `FIND-005` | Medium | The first production record now renders through the schema-backed catalog adapter with route, search, answer-isolation, lab, reference, and additive device-state parity; all five legacy URLs and IDs remain unchanged. | The original first-migration finding is closed. Migrate legacy lessons incrementally after parity checks, and replace eager full-content catalog metadata with a lightweight generated manifest before corpus growth makes it costly. |

## Gaps by program area

| Area | Present now | Missing before complete |
|---|---|---|
| Curriculum | Canonical feature `f2e3e23` provides twenty-six routed identities, including twenty-one schema-backed `LES-0006` through `LES-0026` lessons at `substantive-draft`, 63 assessments, 172 references, plus the full knowledge map. Unpublished `LES-0027` through `LES-0038` are quarantined with direct or static gates passing within their recorded boundaries | Close each draft's independent runtime, safety and representative-evidence blockers before promotion; then complete real-browser QA, formal review, learner evidence, remaining engineering/delivery/Kubernetes/SRE/specialist tracks, real-environment transfers, and scheduled reference review |
| Website | Local launcher, redesigned field-manual shell, journey landing page, routed book, section navigation, context rail, reading desk, code blocks, reading controls, learning modes, validated twenty-six-lesson search/catalog, twenty-one-lesson structured renderer, deterministic 21/63/172 registry, advisory prerequisites, 21 passing reader tests, and device-local reading tools | Real-browser route interaction and visual QA, topic/role/difficulty filters, evidence export, due-review scheduling, incremental legacy migration, browser restart/offline/cross-tab/keyboard/theme/mobile/print validation, full failure UX, and accessibility/performance checks |
| Labs | Bounded ENOSPC fixture source, established Ubuntu/Windows/Docker patterns, guarded local labs including the `LES-0025` CI engines and canonically verified `LES-0026` five-signal fixture, a fail-closed static LES-0027 OpenTelemetry candidate, LES-0028 through LES-0037 teaching models, and a guarded LES-0038 provider-free Terraform/OpenTofu plan fixture with checksum-bound Windows CLI evidence | LES-0027 runtime/safety evidence; LES-0028 through LES-0038 Ubuntu lifecycles plus reviewed representative runtimes, service/team/SLO cases, timed human incidents, governed PIRs, verified actions, authorized capacity/resilience tests, durable idempotency tests, protected state/recovery, provider-backed IaC and complete cleanup matrices; learner-operated evidence and common host/container/VM/Kubernetes harnesses. Local models and provider-free plans are not provider, organizational, contractual, human-performance, causal, benchmark, capacity, correctness, infrastructure or production acceptance evidence |
| Interviews | Role matrix, prompts, detailed answer guides, interactive interview mode, 63 canonical schema-backed assessments, and thirty-three quarantined LES-0028 through LES-0038 assessments with rubric-aligned answer isolation | Timed mocks, role-specific banks, independently reviewed unseen learner answers, scoring calibration, browser interaction, and portfolio defense |
| Reliability evidence | Project ledger, one active learner incident simulation, the canonical bounded observability lesson/lab artifact, and a quarantined SLO decision-system candidate with validated fictional arithmetic | Representative instrumentation/backend and user-journey evidence, approved SLOs and policy outcomes, complete incident program, capacity, DR, projects, and independent learner transfer. Fictional model output is not real reliability evidence |
| Quality | Feature checkpoint `f2e3e23` remains the latest canonical lesson feature at 21/63/172. UI checkpoints `e746eb8` and `481a6cb` pass automated content/schema/reader/lint/type/build and route checks with real-browser QA blocked. LES-0038 direct schema/relationship/rubric checks, checksum-bound Terraform/OpenTofu plan runs, exact-address guarding, ShellCheck, Git Bash syntax, canonical regressions, source review and hygiene pass within the recorded boundary | LES-0038 and earlier quarantined Ubuntu lifecycles are blocked by WSL `0x80070569`; Git Bash lacks usable `python3`. Provider-safe IaC runtime work, protected state recovery, representative resilience testing, browser QA, CI wiring, accessibility/network proof, remaining lab matrices, dependency/license review, formal review and public audit remain |

## Current learner state

- Linux storage exact-path/ENOSPC is recorded at L1.
- The learner correctly recalls that free blocks do not imply free inodes and selects `df -hT <path>` plus `df -i <path>`.
- The learner has not yet produced the required safe remediation, retained-data, recovered-write, cleanup, independent-transfer, or delayed-recall evidence.
- The remaining technical areas are unassessed. Published lessons must not change those entries.

## Next actions

1. Preserve the passing committed `LES-0026` evidence and exact feature parity while checkpointing `LES-0027` only as an explicitly quarantined draft.
2. Close the LES-0027 high-severity runtime-safety findings, derive and review immutable dependencies, split static/runtime verification, then run exact configuration and offline lifecycle evidence before any canonical promotion.
3. Restore Ubuntu 24.04 availability and run the complete LES-0028 through LES-0038 normal-user lifecycles; design reviewed immutable observability runtimes plus representative SRE, capacity, resilience and pinned IaC cases without converting deterministic models into real evidence. Continue the infrastructure sequence with dependency-ready `TFM-002` advanced modules/state/locking/import/refactor/recovery; do not start `DR-001` until its `CLD-001`, `DST-005` and `PLAN-CUR-600` dependencies are substantively available.
4. Keep all canonical structured lessons at `substantive-draft`; complete formal technical/instructional review, independently reviewed learner transfer, delayed recall, and representative real-environment exercises without changing learner levels from project evidence.
5. Migrate legacy lessons incrementally only after route, text, search, answer, lab, and device-state parity checks; add browser-level persistence/keyboard/clipboard/night-mode/mobile/print/visual tests when the browser surface is available.
6. Run the ENOSPC v2 lifecycle, full-boundary tamper/refusal, legacy migration, `check`, `reset`, and cleanup proof in the intended Ubuntu Docker integration.
7. Complete remaining host-lab failure matrices, accessibility/privacy/performance checks, dependency/license review, and CI wiring.
8. Keep the isolated repository active, retain the old OneDrive copy until an explicit retirement decision, and leave learner-operated `PLAN-LAB-102` at its evidence gate.

## Update protocol

After each logical change:

1. update the relevant `MASTER_PLAN.md` status only when its stated acceptance scope changed;
2. append exact results or failures to `VERIFICATION.md`;
3. update this file's current/next/findings sections;
4. update `progress/ledger.md` only if reviewed learner evidence changed;
5. commit and push after validation, preserving unrelated work.
