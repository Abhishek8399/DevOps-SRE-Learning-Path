# LES-0043 deterministic request-path model

Concept rehearsal only: no Kubernetes, network, socket, packet, CNI, DNS, Service, policy or Gateway call.

Run `bash lab.sh doctor`, `setup`, `list`, `diagnose CASE`, `verify-cases`, then `cleanup`. `bash verify.sh` adds wrong-boundary rejection, cleanup refusal and absence proof.
