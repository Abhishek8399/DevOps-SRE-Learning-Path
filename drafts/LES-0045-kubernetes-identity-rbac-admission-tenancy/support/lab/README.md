# LES-0045 deterministic security-gate model

Concept rehearsal only: no cluster, credential, token, Secret, authorization or admission call. Run `doctor`, `setup`, `list`, `diagnose CASE`, `verify-cases`, `cleanup`; `verify.sh` adds wrong-gate and cleanup-refusal checks.
