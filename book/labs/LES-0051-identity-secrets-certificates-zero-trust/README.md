# LES-0051 local identity model

Run `bash lab.sh doctor`, `setup`, `list`, `evaluate CASE`, and `cleanup` as a normal Ubuntu 24.04 user. The fixture contains synthetic booleans and labels only—no token, password, certificate, key, identity or provider credential. `bash verify.sh` checks one allowed baseline, seven denials, one bounded outage decision, hostile-state refusal and cleanup. It is not protocol, cryptographic, provider or production evidence.
