# LES-0044 deterministic storage model

Concept rehearsal only: no cluster, device, mount, storage, snapshot or data operation. Run `doctor`, `setup`, `list`, `diagnose CASE`, `verify-cases`, `cleanup`; `verify.sh` adds refusal and absence checks.
