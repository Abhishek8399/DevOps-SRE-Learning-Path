# LES-0047 local model

This normal-user Ubuntu 24.04 fixture classifies eight extension failures without a cluster, network, credentials or external resources. Run `bash verify.sh`. Passing is model evidence only.
